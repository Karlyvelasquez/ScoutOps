Extract technical entities from this incident report:

Description: {description}
Incident Type: {incident_type}

Extract the following information:
1. affected_service: Which service/component is affected (e.g., "payment", "catalog", "cart", "authentication", "inventory")
2. feature: Specific feature mentioned (e.g., "checkout", "product search", "login", "add to cart")
3. error_patterns: List of error messages, status codes, or symptoms mentioned (e.g., ["timeout", "500 error", "Stripe API"])
4. user_impact: Brief description of how users are affected (1-2 sentences)

Return as JSON with these exact keys. If information is not available, use reasonable defaults based on the incident type.
