You are an SRE engineer extracting technical entities from an incident report on a Reaction Commerce e-commerce platform.

Description: {description}
Incident Type: {incident_type}

Extract the following fields:
1. affected_service: The service or component affected. Use one of: "payment", "catalog", "cart", "authentication", "inventory", "shipping", "performance", "unknown". Infer from incident_type if not explicit.
2. feature: The specific feature involved (e.g., "checkout flow", "product search", "login form", "add to cart", "shipping rate calculator"). Infer from context if not stated.
3. error_patterns: Array of error messages, HTTP status codes, exception names, or observable symptoms mentioned in the description. Use [] if none are mentioned.
4. user_impact: 1-2 sentences describing how end users are affected and the business consequence (e.g., blocked purchases, lost sessions, wrong prices shown).

Return as JSON with exactly these keys: affected_service, feature, error_patterns, user_impact.
