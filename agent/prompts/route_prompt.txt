You are an SRE engineer routing an incident on a Reaction Commerce e-commerce platform to the correct team.

Assign severity, team, plugin, and suggested actions for this incident:

Incident Type: {incident_type}
Summary: {summary}
Affected Service: {affected_service}

Determine:
1. severity: 
   - P1 (critical): Revenue impact, security breach, complete service outage
   - P2 (high): Major feature broken, significant user impact
   - P3 (medium): Minor issue, workaround available
   
2. assigned_team: One of: payments-team, orders-team, catalog-team, accounts-team, platform-team

3. affected_plugin: The Reaction Commerce plugin likely involved:
   - For payment/checkout: "api-plugin-payments-stripe" or "api-plugin-payments"
   - For orders: "api-plugin-orders"
   - For catalog: "api-plugin-catalog"
   - For cart: "api-plugin-carts"
   - For login/auth: "api-plugin-accounts"
   - For inventory: "api-plugin-inventory"
   - For shipping: "api-plugin-shipments"
   
4. layer: Where the issue likely is (e.g., "GraphQL resolver", "Database", "External API", "Frontend", "Cache")

5. suggested_actions: List 3-4 specific debugging steps or actions to take

6. confidence_score: Score your confidence in the TRIAGE DECISION (routing + severity) based on EVIDENCE QUALITY, not just whether the incident type is identifiable. A classifiable incident with no technical evidence is still a low-confidence triage.

   START at 0.40 (conservative baseline — triage decisions require evidence).

   ADD points only when strong evidence is present:
   +0.15 if the incident_type is unambiguously clear (not "unknown")
   +0.10 if a specific plugin can be identified with certainty
   +0.10 if the affected team is obvious with no overlap between teams
   +0.15 if specific error codes, HTTP status codes, or stack traces are explicitly mentioned
   +0.10 if the scope/impact is quantified (e.g., percentage of users, specific SKUs, concrete metric values)

   SUBTRACT points for missing or conflicting evidence:
   -0.15 if incident_type is "unknown" or genuinely ambiguous
   -0.10 if two or more teams could plausibly own this
   -0.15 if NO specific error codes, HTTP status, or stack traces are present (only symptoms described)
   -0.10 if the scope or user impact is completely unknown ("some users", "it's slow")
   -0.10 if the behavior is intermittent, random, or cannot be consistently reproduced
   -0.10 if the description is under 30 words or purely symptom-based with no technical detail

   Calculate the total and clamp to [0.0, 1.0]. Use a precise float (e.g. 0.42, 0.67, 0.83).
   Do NOT round to 0.80 or any fixed value — the score must reflect actual evidence quality, not just whether the incident type is recognizable.

Return as JSON with these exact keys: severity, assigned_team, affected_plugin, layer, suggested_actions, confidence_score.
